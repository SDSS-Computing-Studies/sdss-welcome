function instructions() {
    console.log("Enter in a base and an exponent")
    console.log("use base = 'value'")
    console.log("use exponent = 'value'")
}


/*
example of using the Math.pow function to work with exponents

let base = 4;
let exponent = 1.5;
let answer = Math.pow(base,exponent);

let output = base + " to the power of " + exponent + " is " + answer;
console.log(output);

*/

let x = 13.9999;

let rounded = Math.round(x);

console.log(rounded);

